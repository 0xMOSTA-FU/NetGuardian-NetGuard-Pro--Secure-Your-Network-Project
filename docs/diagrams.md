graph TD;
    A[Launch App] --> B{Select Mode};
    B --> C[Network Sniffing];
    B --> D[Port Scanner];
    B --> E[Vulnerability Check];
    C --> F[Start Capture];
    D --> G[Scan IP Range];
    E --> H[Run Security Audit];
